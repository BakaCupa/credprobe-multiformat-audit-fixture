# CredProbe multi-format audit fixture

This repository contains synthetic, local-only credential examples for testing
CredProbe repository scanning. No real account or external service is used.

The same test environment intentionally uses several common configuration
formats so the scanner can demonstrate repository-wide extraction.
